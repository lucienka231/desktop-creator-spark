import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.przemek.spark',   // ← nowy poprawny App ID
  appName: 'desktop-creator-spark',
  webDir: 'dist'
};

export default config;
